// Core interfaces documented here for reference:
//
// CategoryConfig: { id, name, description }
// Concept: { name, explanation, keyTakeaways[] }
// ExtractionResult: { concepts[], metadata{} }
// QuizItem: { question, options[], correctAnswer, explanation, difficulty? }
// CategoryModule: { config, extract(transcript), generateQuiz(result) }
//
// No runtime exports needed — JS has no interfaces.
// This file exists so the structure is documented in one place.
